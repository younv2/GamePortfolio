//블럭의 기본 데이터(열거형 등)을 정의
namespace Game.Blocks
{
    public enum BlockType
    {
        NA, RED, ORANGE, YELLOW, GREEN, BLUE, SKYBLUE, PURPLE
    }
    public enum BlockState
    {
        NORMAL, MOVE, POP
    }
}
